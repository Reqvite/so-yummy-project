export const selectList = (state) => state.shopping.list;
export const selectIsLoading = (state) => state.shopping.isLoading;
export const selectShoppingListUpateIsLoading = (state) =>
  state.shopping.shoppingListUpateIsLoading;
